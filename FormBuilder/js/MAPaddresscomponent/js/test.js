var ComName = (function() {
  //private 
  var
    config = {},
    win = {},
    toolbar = {};
  //public
  return {
    start: function(_config) {
      // start function
    },
    validate_start: function(_config) {
      // check for all component required fields
    }
  }
}());